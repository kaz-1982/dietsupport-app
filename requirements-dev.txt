# DietSupport CI 用開発ツール(B-17 §4.7.3.1 backend job: ruff / mypy)。
# ランタイム依存は requirements.txt を参照。
#
# バージョンは厳密固定(==)する(B-17 追補 §4.7.9 規定2)。
# 検査ツールは CI の合否そのものを決めるため、版が動くとリポジトリ無変更でも
# 判定が変わる(実例: ruff の既定ルール拡張で 2026-07-07〜08-16 の間、
# コード無変更のまま backend job が赤で放置された)。
# 更新は Dependabot の PR 経由でのみ行う(規定3)。ランタイム依存は対象外。
ruff==0.16.3
mypy==2.3.1
